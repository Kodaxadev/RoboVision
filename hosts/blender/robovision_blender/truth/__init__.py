"""Deterministic Asset Truth: what is measurably true about the geometry.

Everything here measures and nothing here judges taste. A measurement is only
worth something to an unattended loop if two of them can be compared, so each one
is pinned to the state it was taken from and expressed as named metrics and
named invariants rather than as prose an agent would have to parse.
"""
