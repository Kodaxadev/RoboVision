{
  "objective": "Restore Bracket_0 to the common radial radius used by other brackets",
  "reason": "Front silhouettes still show large excess while pattern gaps are now correct; this identifies the remaining likely radial-size fault (one bracket remains at different radius).",
  "targets": [
    {"metric": "excess_fraction", "kind": "reference:front", "epsilon": 0.002}
  ],
  "protected": [
    {"metric": "max_spacing_error", "kind": "pattern:brackets", "tolerance": 0.1}
  ],
  "locality": {"targets": ["Bracket_0"], "protected": [], "allowed": []},
  "operations": [
    {
      "method": "object.transform",
      "params": {
        "object": "Bracket_0",
        "location": [0.204486, 0.0, 0.051205]
      }
    }
  ],
  "advisory": ["reference[front].excess_fraction", "reference[side].excess_fraction", "reference[front].contour_mean_distance"]
}
