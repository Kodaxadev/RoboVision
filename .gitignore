__pycache__/
*.py[cod]
.pytest_cache/
.venv/
dist/
build/
*.egg-info/
.DS_Store
Thumbs.db
.vscode/
.idea/
*.blend1
*.blend2
*.tmp
artifacts/
.unity-compile-refs/
.unity-testbed/
artifacts/
.unity-restart/
.unity-soak/
.unity-ackloss/
.unity-health/
# Throwaway projects for a second editor version, e.g. the declared 6000.0 floor.
.unity-testbed-*/
.unity-restart-*/
.unity-ackloss-*/
.unity-health-*/

# Benchmark answer keys must never reach the public repository, even by
# accident. The real material lives outside it at $RVBENCH_PRIVATE; this is a
# second lock on the door rather than the door.
benchmarks/**/SEALED-*
benchmarks/**/restore.json
benchmarks/**/expected.json
